# 根因分析（RCA）原理剖析 —— 从浅到深

---

## 第一层：它到底在做什么（产品视角）

告警（alert）回答的是"**哪里**在痛"；根因分析回答的是"**谁**造成的、**为什么**、**会波及谁**"。

Ongrid 的 RCA 链路是全自动的：

1. 告警规则评估器发现指标/日志/链路越线 → 产生/更新一条 **incident**（告警事件）；
2. 事件首次触发（isNew）的瞬间，**investigator**（根因调查器）被异步唤起；
3. 调查器派生一个名为 `incident-investigator` 的只读 Agent 工人（worker），
   带着告警元数据进入 **ReAct 推理循环**，反复调用证据工具；
4. 循环收敛后，第二遍 LLM 把自由文本结论**结构化**成报告；
5. 报告落库（`investigation_reports`），前端 IncidentDetail 页渲染：
   **根因 / 因果链 / 证据链 / 影响窗口 / 置信度 / 建议动作**。

一句话定义：

> **RCA = 以告警为"果"，沿时间轴往早找、沿依赖往上游走，
> 直到找到"因"（0 号病人），并用三类独立信号交叉验证。**

对应到官方那句话：

| 官方表述 | 系统里对应的机制 |
|---|---|
| 沿拓扑做爆炸半径分析 | `expand_topology` 工具：在业务拓扑图上做带语义过滤的 BFS，算故障能波及哪些节点（下游）、以及告警节点的上游谁可能是源头 |
| 跨指标/日志/链路 | `correlate_incident` 工具：以告警时刻为中心开窗，一次性拉齐 Prometheus 指标、Loki 日志、Tempo 链路三个面板 |
| 相关到源码行 | `grep_source` / `read_source` / `list_repo_sources` 代码源工具族：把日志/trace 里的 `file:line`、panic 串、报错文本定位到已注册 git 仓库的具体源码行，并沿调用链迭代追读 |

---

## 第二层：系统构成与数据依赖（全景图）

```
                    ┌────────────────────────────────────────────┐
                    │              告警评估器                     │
                    │  (PipelineEvaluator / host evaluators)      │
                    └──────────────┬─────────────────────────────┘
                                   │ incident fires (isNew)
                                   ▼
                    ┌────────────────────────────┐
                    │  investigator.Usecase       │  ← 三道闸门：
                    │  (EnqueueWith)              │    严重度/单事件去重/全局并发
                    └──────────────┬─────────────┘
                                   │ SpawnWorker(incident-investigator)
                                   ▼
        ┌──────────────────────────────────────────────────────────┐
        │            chatruntime worker（ReAct 循环）               │
        │   prompt = 告警元数据 + "先 correlate，再上溯" + 预算约束  │
        │                                                          │
        │   证据工具集（只读）：                                     │
        │   ┌─────────────────┬────────────────────────────────┐   │
        │   │ correlate_      │ 指标: Prometheus (query_range) │   │
        │   │ incident        │ 日志: Loki (LogQL)             │   │
        │   │                 │ 链路: Tempo (TraceQL)          │   │
        │   │                 │ 主机: edge 快照/进程/负载       │   │
        │   ├─────────────────┼────────────────────────────────┤   │
        │   │ expand_topology │ 拓扑: nodes/relations 图        │   │
        │   │ find_topology_  │  (manager DB)                  │   │
        │   │ node            │                                │   │
        │   ├─────────────────┼────────────────────────────────┤   │
        │   │ query_change_   │ 变更: 规则/配置/设备变更事件     │   │
        │   │ events          │                                │   │
        │   ├─────────────────┼────────────────────────────────┤   │
        │   │ query_promql /  │ 三类信号的定向深挖               │   │
        │   │ query_logql /   │                                │   │
        │   │ query_traceql   │                                │   │
        │   ├─────────────────┼────────────────────────────────┤   │
        │   │ get_host_       │ 主机现场: 进程表/负载/大文件     │   │
        │   │ processes ...   │                                │   │
        │   └─────────────────┴────────────────────────────────┘   │
        └──────────────┬───────────────────────────────────────────┘
                       │ 最终答复（Markdown 因果链）
                       ▼
        ┌────────────────────────────┐
        │  Pass-2 结构化抽取          │   单次 LLM 调用，
        │  (report_extractor)        │   Temperature=0，
        │                            │   失败回退首段启发式
        └──────────────┬─────────────┘
                       ▼
        investigation_reports 表 → IncidentDetail 页面
```

（源码：调度与闸门在 `internal/manager/biz/alert/investigator/usecase.go`，
工具在 `internal/manager/biz/aiops/tools/`，
报告抽取在 `internal/manager/biz/alert/investigator/report_extractor.go`。）

**五个外部数据面**，缺一不可但各自可独立降级：

| 数据面 | 提供者 | 在 RCA 中的角色 |
|---|---|---|
| 指标（metrics） | Prometheus | 定量症状 + 时间对齐的"谁先偏离" |
| 日志（logs） | Loki | 定性现场：第一条 ERROR/panic/OOM、错误原因文本 |
| 链路（traces） | Tempo | **唯一的"谁调用了谁"** 运行时证据（caller→callee、慢 span 发起方） |
| 拓扑（topology） | manager DB（nodes/relations） | 静态依赖假设：爆炸半径 + 上游候选集 |
| 源码（code） | Repos-sync 克隆的 git 仓库 | "为什么"的终极落点：报错串 → 具体代码行 |

---

## 第三层：整体逻辑 —— 从告警到报告的流水线

### 3.1 触发与三道闸门（`EnqueueWith`）

`alert.Usecase.recordFire` 在事件**首次触发**（isNew）时调用
`Investigator.InvestigateAsync(incident)`，它转入异步 `EnqueueWith`，依次过闸：

1. **严重度下限**：`severity < MinSeverity`（默认 `warning`）直接静默跳过——
   info/debug 不值得烧 LLM 成本；
2. **单事件去重**：进程内 `inflight` map 以 `incident_id` 为键，
   同一事件在抖动中重复触发只派一个工人；不同事件（即使同规则同设备）并行调查；
   DB 侧 `uniq_invreports_incident` 唯一索引是持久化兜底
   （inflight 只是毫秒级双保险，Create 冲突即跳过）；
3. **全局并发上限**：`MaxConcurrent`（默认 5）信号量。超限不排队，
   直接写一行 `status=skipped`（附原因），让运维立刻看到容量边界。

过闸后**先落 `pending` 行**（前端立刻显示"调查中…"），再 `go uc.run(...)`
脱离告警请求的短命 context 独立跑（`WorkerTimeout` 默认 5 分钟兜底）。

> 设计原则：**RCA 永不阻塞告警管道**。任何失败（spawner 缺失、
> LLM 崩溃、超预算）都只影响报告行状态，不回传到告警链路。

### 3.2 工人启动与初始 prompt（`run` → `renderAlertPrompt`）

`run` 通过 `chatruntime.SpawnWorker` 拉起 `incident-investigator` 人格，
初始用户消息由 `renderAlertPrompt` 渲染，内容刻意很瘦：

- 告警元数据：incident_id / rule / severity / first&last_fired_at /
  device_id / value / threshold / summary / description；
- **第一句指令**：`Start with correlate_incident to pull metrics + logs +
  traces + topology around the fire window. Then drill in...`——
  把"先三信号拉齐、再定向深挖"固化为开局动作；
- **硬预算**：`hard cap 10 tool calls. By tool call #7 you MUST start
  writing the final report...`，同一工具最多调 3 次（`Never call the same
  tool more than 3 times`）、空结果两次即砍分支（历史版本 v0.7.51–55
  的失败全栽在空转试表达式上，这条是踩坑后的硬性约束）；
- **语言指令**：worker 与抽取器 prompt 的语言跟随 locale 解析链
  （live Agent 设置 → 请求 Accept-Language → `ONGRID_DEFAULT_LOCALE`），
  指令置于 prompt 末尾（last-in 更粘）。

### 3.3 推理核心：ReAct 循环 + 方法论（见第四层）

工人按 `agents/incident-investigator.md` 人格里的方法论推进：
定症状 → 排时间线 → 因果上溯 → 递归 → 验证。人格声明 `max_turns: 40`
（eino 图 MaxStep≈82），并 `permission_mode: read-only`、
显式禁用 `execute_skill / host_restart_service / run_shell`——
**调查器只看不动**，任何修复建议只出现在报告文本里。

### 3.4 收敛与补救（salvage）

- 正常完成 → 最终答复进入 **Pass-2 结构化抽取**；
- 若工人撞到 eino 的 MaxStep 上限（`isMaxStepsError`），走
  `salvagePartialAnswer`：从会话记录里把已收集的工具结果拼成
  局部分析，标"置信度偏低"后仍走完抽取——**宁给低置信度部分报告，
  不给空白失败卡**；
- 其他错误 → `status=failed` 并记录原因。

### 3.5 Pass-2：自由文本 → 结构化报告（`extractStructured`）

单次低温（Temperature=0）LLM 调用，输出受 schema 约束的 JSON：

```
root_cause          根因一句话（≤200 rune 截断）
affected_window     影响窗口
pinpoint_target     定位对象（进程/服务/变更项）
evidence[]          证据链（工具 + 结果摘要，分步）
suggested_actions[] 建议动作
confidence          置信度（自动纠正 0–100 / 0–10 漂移并钳到 [0,1]）
```

同批落盘的还有 `tool_call_count`（工人实际工具调用数）与
`confidence_factors`（置信度依据因子，`buildConfidenceFactors` 生成），
供 UI 与自动化验收直接消费。

- `related_alerts`（同时段并发告警）**不依赖 LLM**，纯 DB 查询
  （`buildRelatedAlertsJSON`），与抽取成功与否无关，总是填上；
- `findings_md` 永远保留工人原始全文，抽取只在上面**加结构、不替换**；
- 抽取失败（LLM 错误 / 无 JSON / 解析失败）静默回退到
  `firstParagraphOneLine` 启发式（跳过标题/分隔线/加粗小节头，
  取第一段正文），保证报告永远可读。

### 3.6 补偿路径

- **启动补偿**：`BackfillUnstartedIncidents`——manager 重启时把过去 24h
  内"有告警但没报告行"的事件（典型于配好 LLM 之前的空窗期）
  重新排队，上限 100 条防 LLM 突发风暴；
- **手动重触发**：`POST /v1/alerts/incidents/{id}/investigation` 走
  `ForceEnqueue`：停旧工人、硬删旧报告（含历史软删行，防唯一索引
  1062）、绕过去重闸门重跑——但严重度闸门仍生效
  （info 级即使显式请求也不跑）。

---

## 第四层：推理算法 —— "因果回溯"到底怎么算

这是整个系统最核心的一层。必须先讲清楚一个关键事实：

> **Ongrid 的 RCA 不是一个确定性的图算法（不是 PageRank / random-walk
> 那类评分排序），而是"确定性证据层 + LLM 假设驱动搜索"的混合架构。**

- **确定性部分**（代码写死）：证据的采集范围、时间窗、标签关联、
  拓扑遍历规则、预算闸门、结构化抽取；
- **推理部分**（LLM 驱动）：在确定性证据之上做"提出假设 → 查证 →
  证实/证伪 → 换方向"的循环。

人格文件 `agents/incident-investigator.md` 把这个循环固化为五步工作流，
这就是"算法"本身：

### 步骤 0 —— 按需查知识库
用户明确问 runbook/历史经验，或第一轮证据不足以决定下一步时，
`query_knowledge` 查一次（相似度 ≥ 0.6 才采信）；否则不为了形式先查。

### 步骤 1 —— 定症状、划范围（果，不是因）
`get_incident_detail` 拿规则名 / severity / target / fired_at / labels。
明确认知：**告警是因果链的末端，不是起点**。"CPU 高"不是根因，
"谁把它打高的 + 为什么"才是。

### 步骤 2 —— 排时间线，找首发（核心排序键：时间）
`correlate_incident` 一次拿 metric/log/trace 三件套 + related alerts，
按 `fired_at` / 首次偏离时间排序。**最早偏离的那个才是源头候选**——
下游的高 CPU / 高延迟通常是果不是因。
这是全系统最重要的一条启发式：**别找"最显眼"的，要找"最早"的。**

### 步骤 3 —— 因果上溯一步（每步一个问题、一个工具）
对当前候选问"它的上游/更早一层是谁"，按问题类型选工具：

| 要回答的问题 | 工具 | 信号来源 |
|---|---|---|
| 改了什么？（变更即根因） | `query_change_events`（around_ts=fired_at） | 变更事件库 |
| 依赖的上游是谁？ | `expand_topology`（direction=upstream） | 拓扑图 |
| 调用链上谁先坏？ | `query_traceql`（找最慢/报错 span 的**发起方**） | Tempo |
| 第一条错误是什么？ | `query_logql`（找 fired_at **之前**的第一条 ERROR） | Loki |
| 哪个指标先动？ | `query_promql` | Prometheus |
| 现场是谁在跑？ | `get_host_processes`（pid + 命令行点名） | edge agent |

### 步骤 4 —— 递归上溯，直到触底或信号枯竭
把上一步找到的上游候选当作新的"当前点"，回到步骤 3。终止条件二选一：

- **触底**：再往上没有 in-system 上游（定位到某进程 / 某次变更 /
  某外部依赖）→ 即 **0 号病人**；
- **信号枯竭**：上溯不动 → 输出"目前能到的最深一层 + 还缺什么信号"。
  不允许硬编根因。

### 步骤 5 —— 验证根因（可证伪性）
定位到的源头必须**同时**满足：时间上先于症状、量级/方向吻合下游全链。
对不上就降级为"假设"，报告里明确写置信度与证伪方法
（"什么查询或操作能进一步证实/证伪"）。

### 预算与剪枝（防死循环的硬规则）

- 人格设计预算约 18 次工具调用（够 4–6 层因果跳数），
  运行时 prompt 再压到 **10 次硬顶、第 7 次必须开始写报告**；
- 空结果第一次可换思路，**第二次空立刻砍这条线**；
- 同一工具失败/空 ≥ 2 次必须换工具或换方向，禁止换表达式空转；
- 每调一次工具前先自问"这步能让我更接近源头吗"。

**小结**：所谓"算法"，本质是把人类专家排障的三个核心动作——
**时间排序找首发、沿依赖反向找上游、交叉验证可证伪**——
写成可被 LLM 严格执行的工具调用纪律，而工具层负责把每次取证
变成有界、可降级、标签对齐的确定性查询。

---

## 第五层（深挖一）：沿拓扑做爆炸半径分析

### 5.1 图模型：语义先于名字

拓扑是 manager DB 里的一张**类型化属性图**
（`internal/manager/model/topology/model.go`）：

- **Node**：一切业务实体（设备/服务/集群/应用/机架…）的顶点；
- **Relation**：`(SrcID, DstID, Type)` 唯一的有向边；
- **RelationType**：边的 **AIOps 语义**，带三个关键字段：
  `PropagatesFailure`（故障是否沿此边传播）、`Direction`（往哪边传）、
  `SemanticsTag`（语义桶）。

**推理层只看语义桶，不看关系名**。这样运维自定义一种关系类型
（比如 `fedex_feeds`）只要标对桶，依赖分析**零代码改动**生效。

内置 5 种节点类型（带层级 `Tier`：0 应用 → 4 机架，自上而下即
"业务意图 → 基础设施"）与 7 种关系类型：

| 关系 | 语义桶 | 传播故障 | 故障流向 | 说明 |
|---|---|---|---|---|
| `depends_on` | hard_dep | ✅ | **dst→src** | X 依赖 Y：Y 挂 → X 受影响。**RCA 核心边** |
| `deployed_on` | runtime_dep | ✅ | dst→src | 服务跑在设备上；设备故障上传 |
| `routes_to` | traffic | ✅ | src→dst | LB/网关 → 后端；上游挂下游不可达 |
| `member_of` | aggregation | ❌ | — | 聚合关系，不传播 |
| `replicates_to` | redundancy | ❌ | 双向 | 不直传故障，参与冗余度计算 |
| `monitors` | observation | ❌ | — | 纯观测边 |
| `connected_to` | observation | ❌ | 双向 | 已确认的网络连接 |

注意方向建模的微妙之处：`depends_on` 按"X depends_on Y"存成
`Src=X, Dst=Y`，但故障流是 **dst→src**——
**图上的边方向和故障传播方向不是一回事**，靠 `Direction` 字段声明。

### 5.2 爆炸半径 = 带语义过滤的 BFS

`expand_topology` 工具（`expand_topology_basetool.go`）就是从告警节点
出发的广度优先搜索：

```
visited = {起点: 0 跳}
queue   = [起点]
while queue 非空:
    cur = 出队; hops = visited[cur].hops
    if hops >= depth: continue          # 默认 depth=2，上限钳 5
    for r in allRelations:
        rt = typeMeta[r.Type]
        if only_propagating && !rt.PropagatesFailure: continue   # 语义过滤
        根据 direction 参数决定走下游 / 上游 / 双向
        if 未访问: visited[next] = {hops+1, 关系类型, 语义桶, 经由节点}
结果按 (跳数升序, 名称) 排序，一次 GetMany 批量补水节点详情
```

要点：

- **默认只走传播边**（`only_propagating=true`），所以结果天然就是
  "故障爆炸半径"；想看观测/注释边要显式关掉；
- **方向参数**是爆炸半径与根因回溯的分水岭：
  - `downstream`：故障从这里**会波及谁** → 影响面评估；
  - `upstream`：**谁能造成这里故障** → 根因候选集（investigator 人格
    明确写"顺边往上游走，不是只看 blast-radius 往下"）；
  - `both`（默认）：对称爆炸半径；
- 输出带 `reached_via`（经由哪个方向到达）与 `via_node_name`
  （经由哪个中间节点），等于把**传播路径**一起交还给推理层。

### 5.3 与 RCA 的协作方式

拓扑在因果回溯里的角色是**"给出下一跳该问谁"**：
告警在服务 B → `expand_topology(direction=upstream)` →
得知 B `depends_on` A → 下一轮对 A 取证。
它不产生结论，只产生**有方向的搜索空间**，把"撒网查全集群"
收缩成"只查拓扑上游的几个候选"。

---

## 第六层（深挖二）：跨指标/日志/链路 —— `correlate_incident`

这是"跨三信号"的工程实现（`correlate_incident.go`，754 行），
一次调用把告警现场四个面板打包成一个 JSON 交给推理层。

### 6.1 时间窗

以 `incident.first_fired_at` 为中心，前后各半 `window_minutes/2`
（默认 30 分钟，上限 240）。**所有面板共用同一个窗**——
这正是三信号能"对齐"的前提：时间轴统一。

### 6.2 指标面板（Prometheus）

- 从告警的 `rule_key` 合成 PromQL：闭集指标名（cpu_pct / mem_pct /
  disk_used_pct / load1… ）走 `metricExprFor` 映射成标准
  node_exporter 表达式；映射不到就读标签/注释里的 `metric` 提示；
  都不行 → 面板标记 `skipped: rule kind unsupported`；
- `wrapPerEdge` 用 `and on(edge_id) group by (edge_id)` 把表达式
  锁到告警所在边缘节点；
- `query_range` 拉回后按**全序列最大绝对值排序取前 3**——
  只给推理层最显著的序列，防上下文爆炸。

### 6.3 日志面板（Loki）

```logql
{device_id="<告警设备>"} |~ "(?i)error|panic|oom|fatal|fail"
```

窗口内倒序取 50 条、每行截断 200 字符、带流标签。
日志面板回答"**第一条错误是什么、错误原因的文字表述**"，
也是后续"相关到源码行"的主要线索来源（报错串、stack trace）。

### 6.4 链路面板（Tempo）

- **前提**：incident 的 labels/annotations 里有 `service`；
- 过滤器构造（`traceQLSearchFilters`）：
  `{ resource.device_id = "<id>" && resource.service.name = "<svc>" }`
  ——**设备 + 服务双键**，防止同名服务把别台主机的 trace 拉进来；
- 取窗口内 20 条，只保留 `traceID / rootServiceName / rootTraceName /
  durationMs / startTimeUnixNano` 五个字段。

链路面板回答三信号里**只有它能回答**的问题：
"**谁调用了谁、最慢/报错的 span 是谁发起的**"。
慢调用的根因几乎总是藏在调用链上游某个 span 的发起方，
这是纯指标/日志给不了的结构性证据。

### 6.5 边缘快照 + 并发告警

- `edge` 面板：节点状态/最后心跳/角色 + 三个 3 秒超时的即时探针
  （CPU% / MEM% / up）+ **24h 内该设备告警次数**（抖动计数）；
- `related_alerts`：同窗口内其他并发告警（当前实现为**同设备窗口**，
  拓扑级扇出在 follow-up）——多条告警同时炸时，
  "最早的那条 + 它们的公共上游"往往就是根因。

### 6.6 尺寸预算（防上下文爆炸）

整包 **100KB 硬顶**，超限按噪音从大到小裁剪：
日志先砍到 10 条 → 链路砍到 5 条 → 最后丢指标数值只留标签。
被裁掉的面板写进 `truncated` 字段，让推理层知道自己没看到全貌。

---

## 第七层（深挖三）：相关到源码行

这是整条链的最后一跳：**从"哪个服务/哪行日志"到"哪行代码"**。

### 7.1 实现载体：代码源工具族

`code_source_basetool.go`（HLD-012）提供三个只读工具，
背后是 knowledge biz 的 `CodeBrowser`（对 Repos-sync
持续同步下来的仓库本地克隆做路径安全的读取）：

| 工具 | 作用 |
|---|---|
| `grep_source` | git-grep 正则搜仓库（函数名/类型名/**报错串**），返回 `path:line:text` 命中 |
| `read_source` | 按 `[start_line, end_line]` 窗口读源码；`start_line` 的 schema 描述直接写着 *"Set this to the line from a stack trace"* |
| `list_repo_sources` | 列目录结构，不熟悉仓库时先看骨架 |

### 7.2 关联路径（血缘链）

```
告警/日志里的线索
  ├─ stack trace 的 pkg/foo/bar.go:123  ──→ read_source(path, start_line=123)
  ├─ panic / 报错串（如 "connection refused"）──→ grep_source(pattern)
  └─ 函数/类型名、自然语言描述的行为     ──→ grep_source → read_source
        │
        ▼
  顺着调用链迭代追：对读到的代码里它调用的函数、引用的类型、
  走的报错分支，继续 grep_source 找定义、read_source 跟进，
  逐层理清"输入怎么流到这里、为什么会走到这个分支"
        ▼
  结论落到具体代码行：为什么这个分支会触发、什么条件、谁该改
```

工具文档里把这个过程明确叫**"逻辑探查"**，并强调
"别只读一处就停"——与因果回溯同一个思想：**沿链迭代，直到触底**。

### 7.3 边界与前提（如实说明）

- **前提**：目标仓库必须先在"代码仓库"注册并 sync，否则工具直接
  报 `code browser not configured` / 找不到仓库；
- **当前接线**：`incident-investigator` 人格的只读工具清单
  （见 `agents/incident-investigator.md` 的 `tools:`）**不含**代码源
  工具——自动调查报告给出"服务/进程/变更"级别的根因；
  **源码行级别的追查发生在交互会话**：coordinator 收到报告后，
  顺着日志里的 `file:line` / panic 串，用 `grep_source` →
  `read_source` 把根因钉到代码行。官方表述描述的是**整个平台的能力
  闭环**（自动调查 + 会话追查），而非单指自动工人一步到位。

---

## 第八层（深挖四）：拓扑、日志、Tempo 之间的关系与依赖

### 8.1 各自回答什么问题

| 维度 | 拓扑 | 日志（Loki） | 链路（Tempo） | 指标（Prometheus） |
|---|---|---|---|---|
| 回答的问题 | 谁**可能**影响谁（静态假设） | 现场**发生了什么**（定性） | 调用**实际怎么走**（运行时结构） | 量**变了多少**（定量） |
| 时间性 | 无（静态图） | 逐行时间戳 | 每段 span 起止时间 | 采样点时间序列 |
| 在因果链中的位置 | 提供上游候选集 / 影响面 | 提供"第一条错误"与错误文本 | 提供 caller→callee 与慢 span 发起方 | 提供"谁先偏离"的排序依据 |
| 缺失时的退化 | 只能靠 related_alerts/设备级关联 | 只剩指标+链路，错误原因靠猜 | 丢失跨服务因果结构，只能单机追 | 无法做越线判定与趋势对齐 |

**一句话**：拓扑是**地图**（先验），三信号是**行车记录仪**（后验）；
地图告诉你该去哪查，记录仪告诉你真发生了什么，两者互相校验——
拓扑说"B 依赖 A"，trace 里真有 B→A 的报错 span，日志里真有 A 的第一条
ERROR，指标上 A 确实先偏离——四路对齐，根因才成立。

### 8.2 靠什么"连"起来：标签即连接键

三个信号库互不认识对方，靠**约定一致的标签**在查询层对齐：

| 连接键 | 出现在 | 用途 |
|---|---|---|
| `device_id` / `edge_id` | Prom 标签、Loki 流标签、Tempo `resource.device_id` | 把告警锁到具体主机，三面板同键过滤 |
| `service`（incident 标签/注释） | Tempo `resource.service.name` | 链路面板的准入条件 |
| `trace_id` | 日志行内（demo 服务的 JSON 日志由 `_JsonFormatter` 注入 `trace_id`/`span_id`） | 日志 ↔ 链路的精确跳转键 |
| 时间戳 | 全部 | `first_fired_at` 开窗、"先于症状"验证 |

### 8.3 依赖矩阵与降级行为（代码即事实）

`correlate_incident` 对每个面板独立判空、独立降级，
缺什么写什么 `skipped` 原因，**从不全盘失败**：

| 缺什么 | 面板 | 降级结果 |
|---|---|---|
| 未配 Prometheus | metric_panel | `skipped: prom query client not configured` |
| 规则不是闭集指标且无 `metric` 提示 | metric_panel | `skipped: rule kind unsupported` |
| 未配 Loki / incident 无 device_id | log_panel | 对应 skipped 原因 |
| 未配 Tempo / incident 无 `service` 标签 | trace_panel | `skipped: no service label on incident` |
| 拓扑图为空 | — | `expand_topology` 返回 0 hit 并提示改 `only_propagating=false`，推理层转而靠 related_alerts / 日志上游地址 |
| 仓库未注册 | — | 代码源工具报错，根因停在"服务/进程"级 |

配置生效顺序（三个数据源一致）：**DB 系统设置 > 环境变量 > 默认值**
——这也解释了"Tempo 显示不可用"类故障：默认 `http://tempo:3200`
在容器网络外不可达，必须在设置页或环境变量里改成宿主机可达地址。

### 8.4 Tempo 在整条链里的特殊地位

三信号中，指标和日志都是"**单点**"证据（一个数、一行字），
**只有链路是"关系"证据**：

- 因果回溯的致命问题是"方向"——A 报错和 B 报错同时出现，
  谁因谁果？指标只能看谁**先**，而 trace 直接显示 **A 调了 B**，
  方向是结构性的，不靠猜；
- 跨项目/跨服务故障（如演示环境 order-svc ← inventory-svc）里，
  `peer.service` / caller-callee span 是把根因从"告警方"拉回
  "故障源方"的决定性证据；
- 因此 Tempo 挂了不会让 RCA 失败（面板降级即可），但会把
  跨服务根因的置信度明显拉低——报告里的 `confidence` 与
  "置信度与验证"小节正是为此存在。

---

## 第九层：实战回放 —— 一个真实案例的算法轨迹

用仓库内置的 RCA 演示环境（`demoA` 库存服务 + `demoB` 订单服务）
验证过的完整闭环：

1. **注入故障**：`curl -X POST '127.0.0.1:18001/chaos?mode=slow'`，
   inventory-svc 人为变慢（0.3ms → 5000ms）；
2. **告警触发**：约 45–60 秒后规则
   `sum by (device_id) (rate(order_upstream_errors_total[1m])) > 0.2`
   firing，产生 incident（order-svc 是**告警方**，不是故障源）；
3. **自动调查**：`correlate_incident` 拉齐三信号 → 时间排序发现
   order-svc 的超时晚于 inventory-svc 的延迟抬升 →
   日志面板抓到 order-svc 指向上游 `127.0.0.1:18001` 的报错行 →
   `get_host_processes` 点名 chaos 切换后的进程 →
   因果上溯触底：**0 号病人 = inventory-svc 的慢响应**；
4. **报告落盘**：`status=ready`，root_cause 精确命中
   "inventory-svc 18001 chaos ok→slow 切换时间戳 + duration 跳变 +
   timeout→熔断→502 因果链"，6 次工具调用，三幕变体
   （slow / Redis 挂 / 服务宕）均验证通过。

注意第二幕（Redis 挂）的取证路径换成了
`query_logql` 抓 `127.0.0.1:16379 connection refused`——
**同一个算法框架，证据路径完全自适应**，这正是
"工具确定性 + 推理假设驱动"混合架构的价值。

---

## 第十层：边界、反直觉点与常见误区

1. **没有全局评分排序**。不存在"给每个节点打分、分最高的就是根因"
   的算法；排序键是**时间（谁先偏离）**与**方向（拓扑上游）**，
   由搜索过程动态推进。
2. **图上的边方向 ≠ 故障传播方向**。`depends_on` 存成
   src=依赖方、dst=被依赖方，故障流却是 dst→src，
   全部由 `RelationType.Direction` 声明。
3. **告警节点几乎从不是根因**。它只是因果链末端的路标，
   方法论第 1 步就警告"别停在这"。
4. **"最显眼 ≠ 最根本"**。下游的高 CPU/高延迟通常是最显眼的果，
   源头往往安静得多——所以排序用"最早"不用"最大"。
5. **空结果是证据不是故障**。查询返回空两次就砍分支，
   "没有这种信号"本身要写进报告。
6. **RCA 永不阻塞告警**：所有失败都落在报告行的
   `skipped/failed` 状态上，告警通知链路不受任何影响。
7. **源码行是平台闭环能力**：自动工人止步于服务/进程/变更级，
   源码行由交互会话的代码源工具族接力完成。

---

## 附录：关键源码索引

| 主题 | 位置 |
|---|---|
| 调查调度、三道闸门、工人生命周期、补救路径 | `internal/manager/biz/alert/investigator/usecase.go` |
| Pass-2 结构化抽取与回退启发式 | `internal/manager/biz/alert/investigator/report_extractor.go` |
| 三信号关联打包（窗口/面板/裁剪） | `internal/manager/biz/aiops/tools/correlate_incident.go` |
| 爆炸半径 BFS | `internal/manager/biz/aiops/tools/expand_topology_basetool.go` |
| 拓扑图模型与语义定义 | `internal/manager/model/topology/model.go` |
| TraceQL 设备/服务过滤构造 | `internal/manager/biz/aiops/tools/query_traceql.go` |
| 源码行关联工具族 | `internal/manager/biz/aiops/tools/code_source_basetool.go` |
| 调查员人格与方法论 | `agents/incident-investigator.md` |
| 结构化 RCA 接线（开关/补偿/自监控） | `cmd/ongrid/main.go`（`ONGRID_INVESTIGATOR_ENABLED` 相关段） |
