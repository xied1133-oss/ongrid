# 跨项目根因分析（RCA）演示报告

> 演示目标：验证 Ongrid 平台的结构化根因分析能力 —— 当**项目 B（订单服务 order-svc）**因上游依赖故障触发告警时，平台 RCA Agent 能否仅从 B 侧的告警与症状出发，跨项目回溯到**项目 A（库存服务 inventory-svc）**的真实根因。
>
> 演示主机：device 588（test-host-3）　|　演示时间：2026-08-28　|　结论：**三幕故障全部精确命中根因，回溯成功**。

---

## 1. 演示系统架构

### 1.1 拓扑

```
                    ┌──────────────────────── 主机 device 588 ────────────────────────┐
                    │                                                                 │
 order-loadgen ──▶  │  order-svc (:18002) ──httpx 同步调用──▶ inventory-svc (:18001) ──▶ redis (:16379)
 (每 2s 下一单)     │  (项目 B：告警方)                        (项目 A：故障源)             │
                    │       │                                        │                │
                    │       ▼  OTLP traces / journald 日志 / Prometheus 指标           │
                    └───────┼────────────────────────────────────────┼────────────────┘
                            ▼                                        ▼
              ongrid edge agent（抓取 :18001/:18002 metrics，日志进 journald→Loki，traces→Tempo）
                            ▼
              ongrid Manager（Prometheus/Loki/Tempo + 告警引擎 + RCA Agent）
```

### 1.2 部署形态

- 两个项目各自独立成仓（`demoA/`、`demoB/`），各自 `Dockerfile` + `compose.yml`，容器 `network_mode: host`（直达主机 otelcol 与彼此端口），日志 driver 为 `journald`（容器日志 → journald → Loki）。
- 镜像在 Mac 侧以 `buildx --platform linux/amd64` 构建，`docker save` 打包后经 HTTPS 分发点下发，主机 `docker load` + `deploy.sh` 一键部署。
- 共 4 个容器：`inventory-redis`、`inventory-svc`、`order-svc`、`order-loadgen`。

### 1.3 观测三件套（每个服务同构）

| 通道 | 实现 | 去向 |
|---|---|---|
| 指标 | `prometheus_client`，暴露 `/metrics`，由 edge metrics 插件抓取 | Prometheus |
| 日志 | stdout 单行 JSON，**自动带 `trace_id`/`span_id`**，关键日志带**上游地址、失败原因、业务字段** | journald → Loki |
| 链路 | OpenTelemetry SDK + OTLP HTTP 导出；A 注入 FastAPI/Redis Instrumentor，B 注入 FastAPI/httpx/SQLAlchemy Instrumentor | Tempo |

---

## 2. 项目 A：inventory-svc（库存服务，故障源）

**业务定位**：库存的唯一权威记录方（System of Record），为订单系统提供库存查询与扣减能力。

### 2.1 核心业务逻辑

- **库存存储**：Redis 为 SoR。启动时 `SET NX` 种子库存：`SKU-1001=10000`、`SKU-1002=5000`。
- **原子扣减** `POST /api/v1/inventory/deduct {sku, qty}`：
  1. `DECRBY sku qty`（Redis 原子操作，天然防超卖并发）；
  2. 若结果 `< 0` → 立即 `INCRBY` 回补（撤销本次扣减）并返回 **409 缺货**；
  3. 成功返回剩余库存。
- **库存查询** `GET /api/v1/inventory/{sku}`：供对账与展示。
- **chaos 工程开关** `GET/POST /chaos?mode=ok|slow|error`：模块级故障注入，用于演示——
  - `slow`：每个请求处理前 `await asyncio.sleep(5)`，模拟慢查询 / GC 停顿；
  - `error`：直接返回 500；
  - `ok`：正常。

### 2.2 观测埋点

- 指标：`inventory_requests_total{endpoint,code}`、`inventory_request_duration_seconds`、`inventory_dependency_errors_total{dep="redis"}`、`inventory_stock{sku}`。
- **回溯关键日志**：Redis 不可用时输出 `ERROR redis dependency failure dep=redis error="Connection refused"`；每次扣减输出 `INFO deduct ok sku=... qty=... left=... duration_ms=...`；chaos 切换输出 `WARNING chaos mode changed from=ok to=slow`。

---

## 3. 项目 B：order-svc（订单服务，告警方）

**业务定位**：面向下单流量的订单入口，**同步依赖**项目 A 完成库存扣减后才确认订单。

### 3.1 核心业务逻辑

- **下单** `POST /api/v1/orders {sku, qty}`：
  1. 本地 SQLAlchemy（sqlite）落单，`status=pending`；
  2. 经 httpx 调 A 的 `deduct`（**timeout=2s，不重试** —— 扣减非幂等，重试会超卖，这是真实系统的考量）；
  3. 成功 → `confirmed`（201）；A 业务拒绝（409 等）→ `rejected`；A 基础设施故障 → `failed` + **502**，`reason` 取闭集 `timeout | refused | http_5xx | circuit_open`。
- **熔断器**：连续失败 ≥5 次 → 打开 10s（`order_circuit_open=1`），期间请求直接短路为 `circuit_open`，不再打上游 —— 真实故障传播形态（上游慢/挂 → 本侧熔断 → 流量全部失败）。
- **对账 worker**：每 3s 查一次两个 SKU 的库存。作用：① 提供稳态上游流量，保证故障发生时**即使没有下单流量也有持续错误证据**；② 模拟真实系统的库存对账作业。
- **order-loadgen**：stdlib 下单循环，每 2s 一单（≈0.5 req/s），制造持续业务流量。

### 3.2 观测埋点

- 指标：`order_requests_total{endpoint,code}`、`order_duration_seconds`、**`order_upstream_errors_total{upstream="inventory-svc",reason}`**、`order_circuit_open`。
- **回溯关键日志**：`ERROR upstream deduct failed upstream=http://127.0.0.1:18001/api/v1/inventory/deduct reason=timeout order_id=42` —— 日志里**直接带着故障方 A 的地址与失败原因**，是跨项目回溯的锚点。

---

## 4. 相互影响的业务逻辑与告警设计

### 4.1 故障传播链（设计即真实）

```
A 变慢/依赖挂/进程宕
   → B 的 httpx 调用 timeout / refused / 5xx
   → B 熔断器跳闸（circuit_open）
   → 下单 502 + 对账 ERROR
   → order_upstream_errors_total 速率越线
   → 告警（症状全在 B 侧）
```

告警的**症状**全部表现在项目 B，而**根因**在项目 A —— 这正是生产环境最典型的跨服务故障形态，也是本演示要验证的回溯能力。

### 4.2 告警规则

```promql
sum by (device_id) (rate(order_upstream_errors_total[1m])) > 0.2
```

稳态时该速率为 0；故障时 loadgen（0.5/s）+ 对账（≈0.67/s）的错误流量使速率 ≈1/s，远超阈值，保证 1 个评估周期（30s）内必然触发。

### 4.3 证据链三重冗余（回溯的物质基础）

1. **日志带地址**：B 的 ERROR 日志含 A 的完整 URL 与 reason；A 自身日志含 chaos 切换、duration 跳变、redis 拒连。
2. **指标带标签**：`order_upstream_errors_total{upstream="inventory-svc",reason=...}` 直接点名上游；`reason` 的时序形态（timeout→circuit_open→refused）可区分故障类型。
3. **链路带 peer**：B 的 httpx client span 与 A 的 server span 经 trace_id 串联，A 的 redis span 暴露依赖层。

---

## 5. 三幕故障注入（问题是怎么触发的）

| 幕 | 注入命令（主机执行） | 故障语义 | 恢复命令 |
|---|---|---|---|
| 1 慢查询 | `curl -X POST '127.0.0.1:18001/chaos?mode=slow'` | A 活着但极慢（5s/请求，踩在 B 的 2s 超时线上） | `chaos?mode=ok` |
| 2 依赖挂 | `docker stop inventory-redis` | A 活着、自身依赖（Redis）死亡 → A 对 B 返回 5xx | `docker start inventory-redis` |
| 3 进程宕 | `docker stop inventory-svc` | A 整体消失 → TCP 层拒连 | `docker start inventory-svc` |

三幕覆盖「慢、依赖死、进程死」三种最典型的服务故障形态，且**症状在 B 侧高度相似**（都是上游错误率飙升），考验 RCA 的区分能力。

---

## 6. 平台排查路径与逻辑

### 6.1 机制

1. 告警引擎每 30s 评估规则 → 越线产生 **incident 19**（device 588，critical）。
2. investigator（`ONGRID_INVESTIGATOR_ENABLED=true`）自动 enqueue 调查，RCA Agent 进入工具循环，可用工具：`query_promql`、`query_logql`、`query_traceql`、`get_host_processes`、`query_change_events`、`correlate_incident`。
3. Agent 产出结构化报告（root_cause / 因果链 / 置信度 / 建议动作），约 60–90s 就绪。
4. 同一 incident 持续 firing 不产生新 incident（告警去重），第二、三幕通过 `POST /incidents/19/investigation` 手动触发新一轮调查。

### 6.2 第一幕（慢查询）排查路径 —— 6 次工具调用

1. **correlate_incident**：关联同主机同时段告警，排除孤立噪声。
2. **query_promql**：确认 `order_upstream_errors_total` 速率 0.4987（49.87%）越线；按 reason 拆分见 `timeout` 主导。
3. **get_host_processes**：定位 18001 监听者 = `uvicorn app.main:app --port 18001`（PID 3783313），**进程活着** → 排除「宕机」假设。
4. **query_change_events**：平台侧无变更事件 → 排除发布/配置变更类根因。
5. **query_logql ×2**：
   - A 侧日志：`05:22:04.447 WARNING "chaos mode changed" from=ok to=slow`（**0 号病人事件**），其后 `deduct ok ... duration_ms` 从 0.3–0.4ms 跳到 **5000.8–5001.5ms**；
   - B 侧日志：`05:22:16` 起 `reason=timeout` → `05:22:18` 起 `circuit_open` → `05:22:22` 起 loadgen 502。

**推理结论**：A 的 chaos 切换时间戳早于一切 B 侧错误，且 duration 跳变与切换精确吻合 → 根因 = inventory-svc 被切到 slow 模式。

### 6.3 第二幕（Redis 依赖挂）排查路径 —— 8 次工具调用

1. **query_promql**：错误速率越线，reason 以 `http_5xx` 为主（与第一幕的 timeout 形态不同）。
2. **query_logql（B 侧）**：`upstream deduct failed upstream=http://127.0.0.1:18001/... reason=http_5xx` → 症状锚定到 A。
3. **query_logql（A 侧）**：`ERROR redis dependency failure dep=redis error="Connection refused"` → **A 自身活着，但其依赖 Redis 死亡**。
4. **get_host_processes / 端口验证**：127.0.0.1:16379 无进程监听，连接被拒绝。

**推理结论**：根因 = Redis 未运行（16379 拒连）→ inventory-svc 依赖失败 → 对 order-svc 返回 5xx → 告警。Agent 正确区分了「A 慢」「A 的依赖死」两种形态。

### 6.4 第三幕（进程宕）排查路径 —— 7 次工具调用

1. **get_host_processes**：主机 top-15 进程**无 18001 监听者**（仅有 18002/16379 等）→ 「A 消失」假设成立。
2. **query_logql（edge 侧）**：metrics 插件抓 `http://127.0.0.1:18001/metrics` 报 `dial tcp 127.0.0.1:18001: connect: connection refused`（独立第三方佐证）。
3. **query_logql（B 侧）**：`reason=refused`（TCP 层）+ `reason=circuit_open` 密集 ERROR。
4. **query_promql**：`circuit_open` 计数 43→754 单调增长、`refused` 0→11、`timeout` 早期封顶 50 —— 呈现「timeout → circuit_open → refused」的典型级联失效曲线。

**推理结论**：根因 = inventory-svc 进程下线，18001 拒连 → order-svc 上游调用失败 + 熔断跳开。

---

## 7. 结果

### 7.1 三幕断言汇总

| 幕 | 触发到告警 | 调查工具数 | 根因命中 | 因果链完整 | 建议动作 | 断言 |
|---|---|---|---|---|---|---|
| 1 慢 | 注入后 ~45s（错误率 49.87%） | 6 | ✅ inventory-svc / 18001 / chaos slow（含切换时间戳、PID、duration 跳变） | ✅ timeout→熔断→502 | ✅ | PASS |
| 2 依赖挂 | ~45s | 8 | ✅ Redis 未运行（16379 拒连）→ A 依赖失败 → B 5xx | ✅ | ✅ | PASS |
| 3 进程宕 | ~45s | 7 | ✅ inventory-svc 下线（18001 拒连）→ B 熔断 | ✅ refused/circuit_open 级联 | ✅ | PASS |

### 7.2 根因报告摘录

- **第一幕**：「inventory-svc（PID 3783313，监听 0.0.0.0:18001）在 05:22:04.447 被切换到 chaos mode = "slow"，导致所有请求耗时从 ~0.3ms 暴涨到 ~5000ms（恰好踩在 order-svc 的上游超时阈值上），进而触发 order-svc 的熔断器跳闸，最终引发订单服务上游错误率告警。」
- **第二幕**：「device 588 上 Redis 未运行（127.0.0.1:16379 无进程监听，连接被拒绝），导致 inventory-svc 依赖失败并向上游 order-svc 返回 5xx，触发 order_upstream_errors_high 告警。」
- **第三幕**：「inventory-svc（应监听 127.0.0.1:18001）进程已下线/未运行——主机 588 上没有任何进程监听 18001 端口，TCP 连接直接 connection refused。这是订单服务上游错误率告警的 0 号病人。」并附三层证据互证（进程层 / 日志层 / 指标层）与恢复验证建议（熔断冷却窗口、circuit_open 停止增长作为恢复信号）。

### 7.3 验收口径

断言基于调查报告文本：`status=ready`、根因正则命中（inventory/18001/库存、redis）、因果链含 B→A 传播、含建议动作、工具调用 ≥3。三幕全部通过。

---

## 8. 结论与观察

### 8.1 结论

1. **跨项目回溯成立**：告警症状全部在 B 侧，RCA Agent 三幕均从 B 的指标/日志出发，精确回溯到 A 侧根因，且能**区分三种故障形态**（慢 / 依赖死 / 进程死），给出不同根因表述与处置建议。
2. **证据链设计是关键**：日志带上游地址、指标带 upstream 标签、链路带 trace_id 的三重冗余，使 Agent 在任一通道信息不全时仍能闭环（如第一幕未用 traceql，靠日志 duration 跳变同样命中）。
3. **真实业务形态必要**：不重试、熔断、对账 worker 等真实考量，使故障传播形态（reason 级联曲线）具备可辨识度，RCA 才有「逻辑」可推理，而非靠预设脚本。

### 8.2 产品观察（改进建议）

1. investigator 报告的结构化字段（`evidence`/`suggested_actions`/`confidence`）当前为空，内容沉淀在 `findings_md` 文本中，且 `root_cause` 未剥离模型思考标签 —— 建议后端解析填充，便于 UI 与自动化验收消费。
2. 同规则持续 firing 合并 incident 的去重逻辑下，多故障演示需手动 `POST /investigation` 触发新调查；UI 可对「incident 持续 firing 且症状形态变化」提示重查。

### 8.3 资产留存

- 代码：`demoA/`、`demoB/`（含 compose/deploy.sh/README）；
- 镜像包与代码包保留在 Mac 分发点（`.cache/edge-serve/demo-rca*.tar*`），可随时重演；
- 主机侧已清理（容器/卷/镜像/目录），平台侧告警规则与抓取目标已回退。
