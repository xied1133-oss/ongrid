// Package biz holds the iam BC's use cases and repository interfaces.
//
// Interfaces live here (consumer side); implementations live in ../data.
// Post-pivot the only sub-package is user — orgs and memberships
// were dropped.
package biz
