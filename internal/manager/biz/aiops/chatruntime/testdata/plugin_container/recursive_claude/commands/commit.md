---
description: Commit the staged changes with a conventional commit message.
allowed-tools: [Bash, Edit]
---

Commit the currently staged changes. Generate a conventional commit message
based on the diff. Then run `git status` to show the result.
