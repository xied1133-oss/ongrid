# Ongrid 拓扑详解

> 一句话：**拓扑是平台里的一张"系统地图"**——把设备、服务、集群、应用以及它们之间的
> 依赖关系建成一张**带语义的有向图**。RCA（根因分析）靠它回答两个证据链回答不了的问题：
> **"这个故障还会波及谁？"** 和 **"这个症状可能是谁引起的？"**。
>
> 本文所有论断均对照代码核实（`internal/manager/model/topology/model.go`、
> `internal/manager/biz/aiops/tools/expand_topology_basetool.go` 等），
> 示例数据来自本机真实运行的图（device 588 演示环境，2026-08-30 实测）。

---

## 1. 为什么需要拓扑：指标/日志回答不了的问题

三幕 RCA 演示里，平台靠"证据链"（指标异常 + 日志报错 + 时间对齐）从 order-svc 回溯到了
inventory-svc。这条链能成立，是因为两个服务恰好部署在同一台机器、日志里恰好打印了
上游地址。但换个场景就不行了：

| 场景 | 证据链的困境 | 拓扑能给的 |
|---|---|---|
| order-svc 告警，它有 7 个上游依赖 | 逐个查日志找错误，像大海捞针 | 直接列出 7 个 `depends_on` 候选，按优先级查 |
| 一台宿主机宕机 | 主机自己的指标没异常（它已经死了） | 沿 `deployed_on` 反向边一次找出全部受害服务 |
| 判断"该不该拉响业务级告警" | 只知道某服务 500 | 沿 `member_of` 上溯，知道它属于哪个业务应用 |
| 故障已恢复，要写复盘 | 当时的调用链日志可能已滚动 | 图是静态先验，随时可重放传播路径 |

**核心定位：拓扑是"先验地图"，证据链是"现场勘察"。**
RCA 的正确姿势是：地图先圈定嫌疑范围（谁可能传导故障），证据再逐个证实/排除。
没有地图，agent 只能盲目遍历；只有地图没有证据，只能猜不能证。

---

## 2. 数据模型：三张表构成一张"类型化属性图"

代码位置：`internal/manager/model/topology/model.go`

```
┌───────────────┐        ┌────────────────┐        ┌─────────────────────┐
│   Node（节点） │◀─src── │ Relation（边）  │ ─dst─▶ │   Node（节点）       │
│ id, type,     │        │ src_id, dst_id │        │                     │
│ name, props   │        │ type ──────────┼──▶     └─────────────────────┘
└───────────────┘        └────────────────┘
                                  │ 边自身的语义
                                  ▼
                         ┌─────────────────────┐
                         │ RelationType（边类型）│
                         │ propagates_failure  │  ← 这条边传不传故障？
                         │ direction           │  ← 故障往哪个方向流？
                         │ semantics_tag       │  ← 属于哪类语义桶？
                         └─────────────────────┘
```

三个关键设计：

1. **节点只存图层需要的字段**（type / name / 自由 props 袋）。
   设备、服务各自的详情仍在自己的表里（`devices` / `services`…），
   通过 `node_id` 外键**回指**节点行。所以"两个设备之间""设备与服务之间"的边
   在图层的长相完全一致——图层不关心实体细节。

2. **边是有向的，且 (src, dst, type) 三元组唯一**。
   同一对节点可以有多条边，只要类型不同（如 order-svc 对 inventory-svc
   可以同时有 `depends_on` 和 `monitors`）。

3. **边的"含义"不在边上，在边类型上**。
   推理层从不看边叫什么名字，只看边类型的三个字段
   （传播与否 / 方向 / 语义桶）。这意味着：**运维自定义一种新边类型
   （比如 `queues_into`），只要声明对这三个字段，所有推理逻辑自动对它生效，
   不需要改一行代码。**

### 2.1 设备如何自动进图

每台受管设备在注册 / migrate 时**自动背填**一个 `device` 类型节点
（`devices.node_id` 列关联）。本机实测：

```
device 588 (instance-rusb6kt5) → node 41
device 40  (edd-qms-test0001)  → node 40
device 5   (asp-test0002)      → node 5
```

所以你**永远不需要手建设备节点**，只需补"服务层"的节点和依赖边。
`expand_topology` 工具也支持直接传 `device_id`，内部自动解析到节点再 BFS。

---

## 3. 内置节点类型：5 种，按"层"排列

代码：`BuiltinNodeTypes()`（migrate 每次启动 upsert，可加不可删内置项）。
`tier` 决定 UI 分层布局（0 = 最上层业务，数字越大越贴近裸硬件）：

| tier | 类型 | 中文 | 是什么 | 怎么选 |
|---|---|---|---|---|
| 0 | `app` | 应用 | 跨多服务的**业务能力**（订单系统、支付平台） | 做"业务影响评估"的顶层视角 |
| 1 | `service` | 服务 | **可部署单元**：一个进程 / 容器，一个仓库一个 SLO | 绝大多数组件用这个 |
| 2 | `cluster` | 集群 | **有状态组件**：主备 / 共识 / quorum（MySQL 主备、Redis Sentinel） | 故障语义按"几个副本活着"算 |
| 3 | `device` | 设备 | 物理 / 逻辑主机，edge agent 跑在上面的机器 | 自动背填，不用建 |
| 4 | `rack` | 机架 | 机房 / 机架 / 可用区 | 物理故障域边界（断电、断网） |

**选择口诀：会跑的进程 → service；有状态要凑数（quorum）→ cluster；
一堆服务合起来的业务能力 → app。**

---

## 4. 内置边类型：7 种，三种"传故障"+ 四种"不传"

代码：`BuiltinRelationTypes()`。这是全图最重要的一张表——
**`propagates_failure` 列直接决定推理层会不会沿这条边走**：

| 边类型 | 读法 | 传故障？ | 方向 | 语义桶 |
|---|---|---|---|---|
| `depends_on` | src 依赖 dst | ✅ | **dst→src** | hard_dep（硬依赖） |
| `deployed_on` | src 部署在 dst 上 | ✅ | **dst→src** | runtime_dep（运行时依赖） |
| `routes_to` | src 把流量打到 dst | ✅ | **src→dst** | traffic（流量） |
| `member_of` | src 是 dst 的成员 | ❌ | src→dst | aggregation（聚合） |
| `replicates_to` | src 与 dst 互为副本 | ❌ | 双向 | redundancy（冗余） |
| `monitors` | src 监控 dst | ❌ | src→dst | observation（观测） |
| `connected_to` | 两设备间有网络连接 | ❌ | 双向 | observation（观测） |

### 4.1 方向字段怎么读（最容易绕晕的地方）

`direction` 描述的是**故障流动的方向**，不是画箭头的方向。三种取值：

- **`dst_to_src`（反向流）**——`depends_on` 和 `deployed_on` 都是这个。
  建模约定："X depends_on Y" 记成 `src=X, dst=Y`（主语在前，符合人类读法），
  于是故障流就反过来了：**Y 挂了 → X 遭殃**。

  ```
  order-svc ──depends_on──▶ inventory-svc
                                │ 这里出故障
                                ▼ 故障逆流而上
                            order-svc 遭殃 ✅（三幕演示的真实剧本）
  ```

- **`src_to_dst`（顺向流）**——`routes_to` 是这个：
  网关把流量打给后端，**上游网关挂了 → 下游后端不可达**（故障顺着箭头走）。

- **`bidirectional`（双向）**——`replicates_to`：
  主备任何一边丢数据，另一边都被污染。

### 4.2 为什么 member_of / replicates_to 不传故障

- `member_of` 是**分组**关系（"order-svc 是订单系统的一员"）。
  成员挂了应该**上报告警影响面**（业务视角），而不是说"集群也挂了"。
- `replicates_to` 参与的是**冗余度计算**：副本从 3 掉到 1 是"风险升高"，
  不是"故障正在传播"——真正传故障的是这些副本各自头顶的 `deployed_on` / `depends_on`。

### 4.3 语义桶（semantics_tag）的意义

推理层按桶分发，不按名字。7 个桶：
`hard_dep` / `runtime_dep` / `traffic`（三种"会传导"）
+ `aggregation` / `redundancy` / `observation` / `annotation`（四种"不传导"）。
自定义边类型打上 `hard_dep` 桶，就和 `depends_on` 享受完全相同的推理待遇。

---

## 5. 实例走读：demoA / demoB 的真实图

本机当前运行的图（`GET /api/v1/topology/nodes` / `/relations` 实测）：

```
                        rca-demo (app, 45)          ← tier 0 业务视角
                        ▲            ▲
              member_of │            │ member_of
                        │            │
   order-svc (service, 42)──depends_on──▶ inventory-svc (service, 43)
        │                    │                  │
        │ monitors           │ depends_on       │ deployed_on
        ▼                    ▼                  ▼
   inventory-redis      inventory-redis    instance-rusb6kt5
   (cluster, 44) ◀──────(cluster, 44)     (device, 41)  ← device 588
        ▲                                       ▲
        └──────────── deployed_on ──────────────┘
   order-svc / inventory-svc 也都 ──deployed_on──▶ device 41
```

对应 8 条边的建模理由：

| 边 | 为什么这么建 |
|---|---|
| `order-svc ─depends_on─▶ inventory-svc` | 下单同步扣库存，上游挂了自己 502 —— **这条边就是三幕演示的回溯路径** |
| `inventory-svc ─depends_on─▶ inventory-redis` | 库存 SoR 在 Redis，连接被拒即服务不可用（第二幕的根因路径） |
| 三个组件 `─deployed_on─▶ device 41` | 全跑在 588 上，主机宕机 = 三个一起没（第三幕） |
| `order-svc ─monitors─▶ inventory-redis` | 演示"观测边不传故障"：redis 挂了 order 不会因此被判定故障传导 |
| `order-svc / inventory-svc ─member_of─▶ rca-demo` | 聚合到业务应用，告警可上溯到"哪个业务受损" |

**故障推演**（假设 inventory-redis 44 挂了，推理层怎么走）：

1. 沿 `deployed_on` 反向：41 上还有谁？→ order-svc、inventory-svc 在**同一台机器**（注意：这不等于它们也挂，只是进入候选）；
2. 沿 `depends_on` 反向（dst→src）：谁依赖我？→ inventory-svc ✅ 硬依赖，必受影响；
3. 继续从 inventory-svc 沿 `depends_on` 反向：谁依赖它？→ order-svc ✅；
4. 沿 `member_of` 上溯（聚合边，不判故障）：影响面最终挂到 `rca-demo` 这个业务。

结论：**爆炸半径 = inventory-svc → order-svc（硬依赖链）+ 同主机邻居（待证据确认）**——
与三幕演示第二幕的实际根因完全一致。

---

## 6. 推理层怎么消费这张图

### 6.1 expand_topology：语义过滤的 BFS

代码：`expand_topology_basetool.go`（agent 工具）。工作方式：

1. **起点**：传 `node_id`，或传 `device_id`（自动解析 `devices.node_id` 背填的节点）；
2. **默认只走传故障的边**（`only_propagating=true`）：即 `depends_on` /
   `deployed_on` / `routes_to` 三种；传 `false` 才走全部边（含聚合/观测）；
3. **depth 默认 2，上限 5**；`direction` 可选 `both`（默认，对称爆炸半径）/
   `downstream`（故障从起点往外传的方向）/ `upstream`（谁能引起起点故障）；
4. 输出每个可达节点的：跳数、经由哪条边、边的语义桶、`propagates_failure`、
   从上游还是下游方向到达——**这些字段就是给 LLM 推理用的路标**。

> 注（2026-08-30 修复）：该工具的 `node_id` / `device_id` / `depth` 参数
> 已改用容错类型 `LenientID` / `LenientInt`，容忍小模型把数字传成字符串，
> 否则整轮工具调用报废。

### 6.2 辅助工具

- `find_topology_node`：**按名字子串**搜节点（不区分大小写），用于"用户说了个服务名，
  我要它的 node_id"。注意它只匹配节点名——查设备要用 `query_devices`。
- `get_topology`：整图概览（节点/边计数与清单），用于部署级全局视角。

### 6.3 在 RCA 五步法中的位置

investigator 的工作流是"定症状 → 找首发 → 因果上溯 → 递归触底 → 可证伪验证"。
拓扑参与的是**因果上溯**这一步：给出"候选上游集合"，再由指标/日志逐一验证。
需要强调：**拓扑工具是候选工具**——agent 自主决定用不用。三幕演示第一轮的调查
（13 次工具调用）全走证据链就命中了根因，没碰拓扑；这不代表拓扑没价值，
而是当证据链足够时地图是冗余的。**依赖越多、跨机器越复杂的环境，地图越不可或缺。**

---

## 7. 怎么给一个新系统建图（操作手册）

以"新上一个部署在主机 X 上的支付服务"为例：

**第 1 步：什么都不用做的部分**——主机 X 注册进平台后自动有 `device` 节点。

**第 2 步：建服务节点**（类型按 §3 口诀选）：

```bash
curl -X POST $API/topology/nodes -d '{
  "type": "service", "name": "payment-svc",
  "props": {"port": 18010, "owner": "pay-team"}
}'
```

**第 3 步：连边**（类型按 §4 表选，注意主谓读法）：

```bash
# payment-svc 依赖 redis-cluster（src=主语）
curl -X POST $API/topology/relations -d '{
  "src_id": <payment节点id>, "dst_id": <redis节点id>, "type": "depends_on"
}'
# payment-svc 部署在主机 X 上
curl -X POST $API/topology/relations -d '{
  "src_id": <payment节点id>, "dst_id": <device节点id>, "type": "deployed_on"
}'
```

**建模检查清单**：

- [ ] 每条边能用一句话读出主谓宾（"X depends_on Y" / "X deployed_on Y"）；
- [ ] 依赖方向没反：是"我依赖它"就把**我**放 src；
- [ ] 有状态组件（Redis/MySQL/Etcd）用 `cluster` 不用 `service`；
- [ ] 业务层需要影响面上报时才建 `app` + `member_of`，别为建而建；
- [ ] 建完用 `expand_topology(device_id=X)` 走一遍，确认爆炸半径符合预期。

**维护原则**：服务下线删对应边；拓扑是**低维护成本资产**——
一条 `depends_on` 的有效期以季度计，不需要和每次发布同步。

---

## 8. 边界：拓扑给不了什么

1. **不能代替证据**。图上"可能传导"≠"实际传导"。同主机只是候选，
   到底挂没挂要看进程/指标证据。RCA 结论必须落在证据上。
2. **不能发现未知依赖**。图是人工声明的先验；没建的边，推理层看不见。
   （Tempo 的调用链是它的自动补充源——"谁调用了谁"来自真实流量，
   长期可用 trace 数据校验/反哺人工图。）
3. **不感知运行时状态**。图不区分"这个副本现在活着"——那是指标/探针的事。
4. **方向解读仍依赖模型**。实测中 agent 曾把 `depends_on` 的传播方向
   讲反过（工具输出的方向字段是齐全的，属解读偏差）——消费结论时
   对照 §4.1 核一眼方向。

---

## 附：一图总结

```
        证据链（指标+日志+trace）      拓扑（本文主角）
        ─────────────────────       ─────────────────
角色     现场勘察                     先验地图
回答     发生了什么 / 什么时候         还会波及谁 / 谁可能引起
时效     窗口内有效                   长期有效
短板     依赖多时大海捞针             未知依赖看不见
合流点   RCA：地图圈定嫌疑 → 证据证实/排除
```
