运行命令：`cd web && npm run test:e2e`。需要设置 `ONGRID_ADMIN_EMAIL`/`ONGRID_ADMIN_PASSWORD`（或 `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD`）以及可选的 `E2E_BASE_URL`；默认使用本地 Chrome，可通过 `E2E_BROWSER_CHANNEL` 切换浏览器通道；报告与截图输出到 `output/playwright/`。