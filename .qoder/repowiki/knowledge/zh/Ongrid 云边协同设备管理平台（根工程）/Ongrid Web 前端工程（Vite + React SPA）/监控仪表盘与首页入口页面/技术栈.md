React + TypeScript；图表使用 `recharts`（BarChart/PieChart/ResponsiveContainer）与内联 SVG；图标来自 `lucide-react`；状态轮询通过自研 `@/lib/usePoll`；国际化通过 `@/i18n/locale` 的 `tr()`；PromQL 查询经 `@/api/client` 的 `promQueryRange` 与 `injectDeviceIDFilter` 处理。