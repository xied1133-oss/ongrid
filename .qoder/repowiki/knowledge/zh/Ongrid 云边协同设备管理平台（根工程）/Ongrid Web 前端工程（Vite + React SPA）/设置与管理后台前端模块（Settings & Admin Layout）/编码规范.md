- 布局文件以常量数组（RAIL_ITEMS/railItems）声明左侧导航项，每项包含 to、icon、label/hint 等元数据，子页面通过该数组注册而非硬编码 NavLink。
- 所有设置与管理页面默认仅对管理员开放，布局层集中检查 usePermissions().isAdmin 并返回 EmptyState，子页面不再重复做顶层权限拦截。
- 子路由采用懒加载并通过布局内局部 Suspense 包裹 Outlet，避免路由切换时卸载整个 shell。
- 导航项文案与提示文案同时提供中英文键，统一通过 tr('zh', 'en') 读取以实现 i18n。