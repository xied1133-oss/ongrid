React + TypeScript 函数组件；Tailwind CSS 样式；lucide-react 图标；i18n 通过 `@/i18n/locale` 的 `tr` 键值对；UI 基础组件来自 `@/components/ui`（Button/Card/Chip）和 `@/components/Modal`；后端 API 封装在 `@/api/settings`、`@/api/alerts`、`@/api/version`。