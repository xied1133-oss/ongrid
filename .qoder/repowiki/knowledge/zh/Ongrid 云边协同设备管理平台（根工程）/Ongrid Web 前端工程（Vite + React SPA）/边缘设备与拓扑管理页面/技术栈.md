React + TypeScript 函数组件；recharts 用于指标折线图；WebSocket + xterm（via `@/components/XTerminal`）实现 WebSSH；PromQL 查询通过 `promQueryRange` 拉取 node_exporter 指标；路由基于 react-router-dom 的 `useLocation`/`useNavigate`/`useParams`。