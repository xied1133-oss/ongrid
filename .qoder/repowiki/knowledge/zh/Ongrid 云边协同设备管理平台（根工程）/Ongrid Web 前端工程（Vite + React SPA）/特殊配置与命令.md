`npm run dev` 启动 Vite 开发服务器；`npm run build` 先执行 `tsc -b` 类型检查再打包；`npm run test` 运行 Vitest 单测；`npm run test:e2e` / `test:e2e:headed` 运行 Playwright 端到端用例；`npm run lint` 与 `npm run typecheck` 分别执行 ESLint 与 TypeScript 类型检查。