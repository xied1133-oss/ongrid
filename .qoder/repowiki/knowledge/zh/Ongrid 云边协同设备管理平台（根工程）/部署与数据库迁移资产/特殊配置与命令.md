本地开发：`cp .env.example .env && make compose-up`（阻塞等待 MySQL healthy 后启动完整栈）。生产安装：解压发布包后 `sudo ./install.sh`，升级用 `sudo ./upgrade.sh`，卸载用 `sudo ./uninstall.sh [--purge --yes]`。数据库迁移：`DB_DSN='...' make migrate-up` 或 `migrate-down`。Edge 安装：从云端获取 access_key/secret_key 后在目标主机执行 `./install-edge.sh`。