构建时通过 `-ldflags -X main.version=...` 注入版本号；运行前必须设置 `ONGRID_JWT_SECRET` 为非默认值否则直接退出；可选环境变量包括 `ONGRID_OTEL_ENDPOINT`（默认 tempo:4318）、`ONGRID_AUDIT_RETENTION_DAYS`、`ONGRID_GRAFANA_PANEL_DASHBOARD_UID`、`ONGRID_EDGE_BUNDLE_DIR`、`ONGRID_ADMIN_EMAIL/PASSWORD` 等。