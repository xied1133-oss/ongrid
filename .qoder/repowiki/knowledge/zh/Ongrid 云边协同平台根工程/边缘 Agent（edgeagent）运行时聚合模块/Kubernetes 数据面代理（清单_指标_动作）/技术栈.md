Go 标准库 net/http + crypto/tls 直连 kube-apiserver（ServiceAccount token + CA），使用 JSON Merge Patch (`application/merge-patch+json`) 与原生 JSON 调用 REST；watch 启用 `allowWatchBookmarks=true` 与超时；通过 `internal/pkg/k8sredact` 对敏感标签/消息脱敏。