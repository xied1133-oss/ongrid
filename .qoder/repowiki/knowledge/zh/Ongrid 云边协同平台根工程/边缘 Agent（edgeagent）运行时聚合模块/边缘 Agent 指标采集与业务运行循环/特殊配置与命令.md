Scrape 模式需设置环境变量 `ONGRID_EDGE_COLLECTOR_MODE=scrape` 并准备 `/etc/ongrid-edge/scrape.yaml`；Kubernetes 模式下禁用 packet capture（构造时直接报错）；远程升级需在 `Config.UpgradeStageDir` 指定目录以启用 `MethodAgentUpgrade/FetchPackage/ApplyPackage` 处理器。