- 通过 `//go:build windows` 与 `//go:build !windows` 双文件实现同一包的跨平台接口，非目标平台返回专用 sentinel error。
- 对外函数统一使用 `fmt.Errorf("...: %w", err)` 包装底层错误，保留错误链以便调用方诊断。
- 配置参数通过常量 + clamp 函数限制取值范围（如 `rotationDays` 钳制到 `[30, 365]`），默认值集中定义在包级常量中。