bash + set -euo pipefail 为主，配合 jq 处理 JSON、Python 做 YAML 校验（`test-release-workflow.sh` 用 Ruby/YAML 解析 GitHub Actions workflow）、`docker buildx imagetools inspect` 检查多架构镜像；CNB 通过 `api.cnb.cool` REST API（`application/vnd.cnb.api+json`）交互。