运行脚本前需设置 `CNB_TOKEN`（CNB Release 读写权限）及可选 `CNB_API_ENDPOINT`、`RELEASE_IMAGE_CHECK_ATTEMPTS`、`RELEASE_IMAGE_CHECK_RETRY_DELAY`；`test-release-workflow.sh` 需要系统安装 `ruby` 用于解析 `.github/workflows/release.yml`。