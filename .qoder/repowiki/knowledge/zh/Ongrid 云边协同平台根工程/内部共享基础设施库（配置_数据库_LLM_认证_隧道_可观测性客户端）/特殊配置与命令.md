无特殊构建命令；运行期通过 `ONGRID_*` 环境变量（见 `config.Load`）注入全部配置，例如 `ONGRID_DB_DIALECT=mysql|sqlite`、`ONGRID_OPENAI_API_KEY`、`ONGRID_PROM_ENABLED`、`ONGRID_LOG_QUERY_URL=http://loki:3100`、`ONGRID_TRACE_QUERY_URL=http://tempo:3200` 等；SQLite 测试可使用 `:memory:` 路径。