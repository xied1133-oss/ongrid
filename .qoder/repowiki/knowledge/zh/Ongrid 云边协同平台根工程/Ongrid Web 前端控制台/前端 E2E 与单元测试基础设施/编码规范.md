- E2E 登录流程统一封装在 `helpers.login`，并通过 `page.context().storageState` 将认证状态持久化到 `output/playwright/.auth/admin.json` 供后续用例共享。
- 每个 spec 通过 `trackPageErrors(page)` 订阅 `pageerror` 事件，在断言末尾校验页面未产生 JS 错误，确保 UI 交互不会静默失败。
- 需要确定性的 E2E 用例使用 `page.route('**/api/v1/**', ...)` 拦截请求并以 `route.fulfill({ json })` 返回固定 fixture，再对 `request.postDataJSON()` 做结构断言以验证提交内容。
- 单测通过 `src/test/setup.ts` 全局启动 MSW server 并设置 `onUnhandledRequest: 'error'`，每个测试文件自行调用 `server.use(...)` 注册 handler，并在 `afterEach` 自动清理。
- 国际化与主题相关断言使用正则匹配中英文文案（如 `/邮箱|Email/`、`/仪表盘|Dashboard/`），以兼容双语界面。
- 测试数据集中在 `fixtures/` 下并按后端 API 返回的 wire shape 定义 TypeScript 类型，避免在 spec 中散落硬编码对象。