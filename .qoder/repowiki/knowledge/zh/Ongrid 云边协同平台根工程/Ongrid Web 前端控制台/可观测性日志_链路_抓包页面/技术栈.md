React + TypeScript 页面组件；recharts 用于日志直方图；lucide-react 图标；react-router-dom 路由；通过 `@/api/logs`、`@/api/traces`、`@/api/packetCaptures` 等 API 客户端访问后端 Loki/Tempo/抓包服务；Grafana Tempo Explore 通过 `buildExploreUrl` 深度链接。