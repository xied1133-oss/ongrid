Go + chi/v5 HTTP 路由；JWT 使用 golang-jwt/jwt/v5 签发 access/refresh token；密码哈希使用 argon2id（biz/user/hash.go）；RBAC 授权使用 casbin/casbin/v2 + gorm-adapter 将策略持久化到 `casbin_rule` 表；ORM 使用 gorm.io/gorm，SQLite 作为默认存储。