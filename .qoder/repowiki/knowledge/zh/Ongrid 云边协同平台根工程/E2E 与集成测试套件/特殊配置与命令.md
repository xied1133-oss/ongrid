需启用 `e2e` 构建标签运行：`go test -tags=e2e ./tests/e2e/...` 或通过 `make test-e2e`；要求 Docker 守护进程可达且分配 ≥4 GiB 内存；默认禁用 testcontainers ryuk（`TESTCONTAINERS_RYUK_DISABLED=true`）；live 模式通过设置 `E2E_LIVE_SLACK=1` 等环境变量切换，凭据从 `tests/e2e/secrets.local.env` 或 CI 注入的环境变量加载。